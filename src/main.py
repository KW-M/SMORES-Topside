"""Entry point for the SMORES-Topside backend.

Placeholder for step 1 (project scaffold). Lifecycle management, config
loading, and subsystem wiring are implemented in a later step per
AGENTS.md.
"""

import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


def main() -> None:
    logger.info("SMORES-Topside scaffold OK. Lifecycle not yet implemented.")


if __name__ == "__main__":
    main()
